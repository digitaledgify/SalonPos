import { ExpenseItem } from '../types/expense';

// All demo/seed expenses have been cleared for production use. Add real
// expenses from within the Expenses module.

export const INITIAL_EXPENSES: ExpenseItem[] = [];
