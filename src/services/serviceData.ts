import { SalonService } from '../types/service';

// All demo/seed services have been cleared for production use. Add real
// services from within the Services module.

export const INITIAL_SERVICES: SalonService[] = [];
